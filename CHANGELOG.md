# 更新日志

本文件用于记录每个发布版本的变化。GitHub Actions 会读取**第一个** `## vX.Y.Z`
标题下的内容作为 Release 说明，因此新增版本时请把新段落插在最上面。

## v1.0.1

一轮全量遍历（140 条内置物质 + 130 条 SMILES 语料）找出的两个真实缺陷。

### 修复

- **腈类被误判成聚合度**：`CH3CN`（乙腈）曾被解析成 `CH₃Cₙ` 并提示
  "聚合度 n 不定"，`CH2=CHCN`（丙烯腈）、`NN` 同样受害。原因是聚合度
  变量下标字母集里混进了**大写** `N`/`M`/`X`，于是"无下标的碳紧跟氮"
  被吃成了下标。现在只认小写 `n`/`m`/`x`（聚合度按惯例就是小写，
  如 `(C6H10O5)n`），大写字母一律按元素符号处理。
- **离子化合物给出误导性的拒绝理由**：`氯化钠`、`硫酸铜`、`碳酸钠`、
  `高锰酸钾`、`苯甲酸钠` 等由互不相连的组分构成的物质，以前会报
  "最大键长偏差 0.85 / 最近的非成键原子距离 0.34" 这种让人摸不着头脑的
  几何理由，个别（`氯化钠`、`氢氧化钠`）甚至会被判为可绘制、画出一张
  没有意义的图。现在统一识别为多组分，明确说明"离子化合物或混合物，
  键线式不适用"。

### 其它

- README 补充说明：纯 ASCII 文本中电荷与键符号存在歧义
  （`O⁻NO₂` 会渲染成 `O-NO2`，回解析时丢电荷）；图片与 Unicode 形式
  使用上标字符，不受影响。
- 测试新增 13 组回归用例（腈类 5 组、离子化合物拒绝 4 组、渲染拒绝 2 组、
  解析断言 2 组），并在 Python 3.12 与 3.14 上分别通过。

## v1.0.0

首个公开版本。

### 功能

- **结构简式**：从分子图按中学教材约定生成连写式简式（省略单键、只保留
  碳碳重键、支链写在主链延续之前、羧酸与酯从酰基一侧写起、苯环按苯基展开）。
  生成后会回算分子式与原分子比对，不一致就拒绝输出。
- **键线式**：自研纯 Python 引擎绘制骨架式，不依赖 RDKit。
  - `chem/layout.py`：SSSR 环识别（GF(2) 线性无关性筛选）、正多边形摆环、
    稠环/螺环/联苯三种拼接方式、±60° 锯齿链、键长弹簧与斥力松弛，
    失败时用确定性多起点重试。
  - `chem/render_skeletal.py`：杂原子标注与 CPK 配色、环内外双键差异、
    三键、电荷、凯库勒化（最大匹配）。
  - 环系在平面上几何无解时（如桥环）**拒绝出图并说明原因**，不输出错误结构。
- **结构式**：可选 RDKit 渲染；未安装时自动降级到内置键线式引擎。
- **四类输入自动识别**：中文名/俗名、SMILES、结构简式、分子式，
  并对 `C2H6O` 这类同分异构体列出全部候选。
- **内置词表**：140 条常见物质，分 26 个类别。
- **WebUI 预览页**：结构简式图、键线式图、结构式、分子信息与词表浏览。
- 图片缓存、配置项热更新与脏数据兜底。

### 指令

`/化学 结构|简式|分子式|键线式|结构式|列表|状态|帮助`，以及顶层快捷方式
`/结构 <物质>`。

### 已知限制

- 环状、杂环、稠环物质没有连写式简式，会自动改用键线式。
- 桥环与超过 36 个重原子的柔性分子可能被拒绝绘制。
- 不表达立体化学（手性中心的楔形/虚线）。
